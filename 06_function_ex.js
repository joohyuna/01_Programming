// 함수 : 선언문을 통해 만든 함수 라고 합다
// 매개 변수가 없는 함수
function sayHello() {
    console.log("안녕하세요!");
}
// 함수 뒤에 추론 반환값에 대한 추론 void 반환값이 없을때 나온다. 값이 없다.
// function 의 이름 소문자로 시작
// 사용

// 그렇게 만든 함수 실행문 "호출" (싷행)
